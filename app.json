{
  "name": "Leech Bot",
  "description": "A Telegram bot to leech content from provided links.",
  "repository": "https://github.com/chamanprahs/Bruno",
  "keywords": ["telegram", "bot", "leech"],
  "env": {
    "BOT_TOKEN": {
      "description": "The token for your Telegram bot from BotFather",
      "required": true
    },
    "DATABASE_URL": {
      "description": "URL for the database (e.g.,MongoDB, MySQL)",
      "required": true
    },
    "API_ID": {
      "description": "API ID obtained from the Telegram API",
      "required": true
    },
    "API_HASH": {
      "description": "API hash obtained from the Telegram API",
      "required": true
    },
    "OWNER_ID": {
      "description": "Telegram ID of the bot owner",
      "required": true
    },
    "HEROKU_APP_NAME": {
      "description": "The name of your Heroku app",
      "required": true
    },
    "HEROKU_EMAIL": {
      "description": "Your email address for Heroku",
      "required": true
    },
    "HEROKU_API_KEY": {
      "description": "Your Heroku API key",
      "required": true
    },
    "STRING_SESSION": {
      "description": "String session for the bot (optional)",
      "required": false
    }
  },
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ]
}
